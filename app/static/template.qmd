---
params:
  title: NULL
  description: NULL
  date: !expr Sys.Date()
  dataset: NULL
---

# `r params$title`
*Report time:* `r params$date`
`r params$description`

## Dataset

The dataset used in this report is `r deparse(substitute(params$dataset))`.

It has `r nrow(params$dataset)` rows spread across the following columns:

`r names(params$dataset)`

A preview of it is given below:

`r params$dataset`
